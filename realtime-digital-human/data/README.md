# realtime runtime data

Extract `data.zip` into this directory before running the realtime digital human service.

After extraction, the default avatar should be available at:

```text
data/avatars/wav2lip_avatar11/
```

The extracted runtime data is not committed to Git because it contains large generated avatar resources. See the root `README.md` for the Baidu Netdisk download link.
